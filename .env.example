# ---- Supabase (source of truth + Realtime) ----
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=

# ---- OpenAI (Realtime voice, vision, agent tools) ----
OPENAI_API_KEY=
OPENAI_AGENT_MODEL=gpt-4o-mini
OPENAI_VISION_MODEL=gpt-4o-mini
OPENAI_REALTIME_MODEL=gpt-realtime-2.1-mini

# ---- OpenRouter (fallback if OpenAI keys are rate-limited) ----
OPENROUTER_API_KEY=

# ---- Telegram (approvals + alerts) ----
TELEGRAM_BOT_TOKEN=
# random string; Telegram sends it back as X-Telegram-Bot-Api-Secret-Token
TELEGRAM_WEBHOOK_SECRET=
# chat id of the demo relative ("Claire")
TELEGRAM_APPROVER_CHAT_ID=

# ---- Stripe (TEST mode only) ----
STRIPE_SECRET_KEY=sk_test_
STRIPE_PAYMENT_METHOD=pm_card_visa

# ---- Auth0 (identity + approver role; budget lives in the DB) ----
AUTH0_SECRET=
AUTH0_BASE_URL=http://localhost:3000
AUTH0_ISSUER_BASE_URL=
AUTH0_CLIENT_ID=
AUTH0_CLIENT_SECRET=

# ---- Trigger.dev (expiry notice, escalation, refill) ----
TRIGGER_SECRET_KEY=
TRIGGER_PROJECT_REF=

# ---- Exa (find pharmacy — stretch) ----
EXA_API_KEY=

# ---- App ----
# production URL, used for the Telegram webhook and Stripe return_url
APP_URL=http://localhost:3000
# demo seed
DEMO_ELDER_ID=marie
DEMO_APPROVER_ID=claire
DEMO_WEEKLY_LIMIT_CENTS=6000
